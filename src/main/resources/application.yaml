spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booking?serverTimezone=UTC
    username: user
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect

server:
  port: 9701
  servlet:
    context-path: /api